<!-- Generated by scripts/generate_cli_reference.py; do not edit. -->
# Palimpsest CLI syntax reference

This file is generated from `palimpsest_local.cli.build_parser`. It inventories every command, positional, option, choice, and parser default. For behavior, examples, and runtime restrictions, read [the usage guide](usage.md). Every command except raw `docker` pass-through accepts automatic `-h`/`--help`.

Coverage: **74 command paths**, **216 positional/option definitions** (excluding automatic `-h/--help`).

## `palimpsest`

[Behavior, restrictions, and examples](usage.md#invocation-and-configuration).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest [-h] [--url URL] [--version]
                  {image,layer,bundle,oci,build,registry,login,logout,pull,push,tag,images,history,rmi,save,load,docker,run,compose,ps,inspect,logs,shell,exec,start,stop,rm,commit,ui,store,completion} ...
```

Subcommands: `image`, `layer`, `bundle`, `oci`, `build`, `registry`, `login`, `logout`, `pull`, `push`, `tag`, `images`, `history`, `rmi`, `save`, `load`, `docker`, `run`, `compose`, `ps`, `inspect`, `logs`, `shell`, `exec`, `start`, `stop`, `rm`, `commit`, `ui`, `store`, `completion`.

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--url` | string | `None` | Override the Palimpsest Hub base URL. |
| `--version` | flag | — | show program's version number and exit |

## `palimpsest image`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image [-h]
                        {ls,pull,verify,import,push,inspect,history,rm,save,load} ...
```

Subcommands: `ls`, `pull`, `verify`, `import`, `push`, `inspect`, `history`, `rm`, `save`, `load`.

### `palimpsest image ls`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image ls [-h] [--ubuntu-base UBUNTU_BASE] [--arch ARCH]
                           [--os-variant OS_VARIANT]
                           [--disk-format {qcow2,raw}] [--limit LIMIT]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--ubuntu-base` | string | `None` | Filter or record the Ubuntu base release. |
| `--arch` | string | `None` | Filter or record the image CPU architecture. |
| `--os-variant` | string | `None` | Filter or record the libvirt OS variant. |
| `--disk-format` | one of: `qcow2`, `raw` | `None` | Select the cloud-image disk format. |
| `--limit` | int | `50` | Maximum results to return (valid range: 1–200). |

### `palimpsest image pull`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image pull [-h] [--output OUTPUT] digest
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `digest` | string | required | Expected or selected SHA-256 content digest. |
| `--output` | Path | `None` | Write or copy output at this path. |

### `palimpsest image verify`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image verify [-h] --digest DIGEST path
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `path` | Path | required | Local image file whose bytes must match &#96;--digest&#96;. |
| `--digest` | string | required | Expected or selected SHA-256 content digest. |

### `palimpsest image import`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image import [-h] --disk-format {qcow2,raw}
                               --arch {x86_64,aarch64}
                               [--os-variant OS_VARIANT]
                               path
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `path` | Path | required | Local qcow2/raw cloud-image file to import into the local store. |
| `--disk-format` | one of: `qcow2`, `raw` | required | Select the cloud-image disk format. |
| `--arch` | one of: `x86_64`, `aarch64` | required | Filter or record the image CPU architecture. |
| `--os-variant` | string | `None` | Filter or record the libvirt OS variant. |

### `palimpsest image push`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image push [-h] --name NAME [--disk-format {qcow2,raw}]
                             [--arch {x86_64,aarch64}]
                             [--os-variant OS_VARIANT]
                             [--ubuntu-base UBUNTU_BASE] [--publish]
                             path
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `path` | Path | required | Local qcow2/raw cloud-image file to upload to Hub &#96;/v1&#96;. |
| `--name` | string | required | Managed object, run, profile, or publication name. |
| `--disk-format` | one of: `qcow2`, `raw` | `'qcow2'` | Select the cloud-image disk format. |
| `--arch` | one of: `x86_64`, `aarch64` | `'x86_64'` | Filter or record the image CPU architecture. |
| `--os-variant` | string | `None` | Filter or record the libvirt OS variant. |
| `--ubuntu-base` | string | `None` | Filter or record the Ubuntu base release. |
| `--publish` | flag | `False` | Make the uploaded Hub artifact publicly visible. |

### `palimpsest image inspect`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image inspect [-h] [-f FORMAT] [--platform PLATFORM]
                                [--registry REGISTRY]
                                references [references ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `references` | string (one or more) | required | One or more Docker image references or IDs. |
| `-f`, `--format` | string | `None` | Select the output representation or Docker format template. |
| `--platform` | string | `None` | Select an OCI platform. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

### `palimpsest image history`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image history [-h] [--format FORMAT] [--no-trunc]
                                [--platform PLATFORM] [-q]
                                reference
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `reference` | string | required | Docker image reference or ID. |
| `--format` | string | `None` | Select the output representation or Docker format template. |
| `--no-trunc` | flag | `False` | Do not truncate Docker output. |
| `--platform` | string | `None` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |

### `palimpsest image rm`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image rm [-h] [-f] [--no-prune] [--platform PLATFORM]
                           references [references ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `references` | string (one or more) | required | One or more Docker image references or IDs. |
| `-f`, `--force` | flag | `False` | Replace, remove, or overwrite despite the normal protection. |
| `--no-prune` | flag | `False` | Do not delete untagged parents. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |

### `palimpsest image save`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image save [-h] [-o OUTPUT] [--platform PLATFORM]
                             references [references ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `references` | string (one or more) | required | One or more Docker image references or IDs. |
| `-o`, `--output` | Path | `None` | Write or copy output at this path. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |

### `palimpsest image load`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest image load [-h] [-i INPUT_PATH] [--platform PLATFORM] [-q]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `-i`, `--input` | Path | `None` | Read the Docker image archive from this file instead of stdin. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |

## `palimpsest layer`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest layer [-h] {ls,pull,pack,push} ...
```

Subcommands: `ls`, `pull`, `pack`, `push`.

### `palimpsest layer ls`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest layer ls [-h] [--name NAME] [--kind KIND] [--parent PARENT]
                           [--limit LIMIT]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--name` | string | `None` | Managed object, run, profile, or publication name. |
| `--kind` | string | `None` | Filter by artifact kind. |
| `--parent` | string | `None` | Record or filter by the parent layer digest. |
| `--limit` | int | `50` | Maximum results to return (valid range: 1–200). |

### `palimpsest layer pull`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest layer pull [-h] [--output OUTPUT] digest
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `digest` | string | required | Expected or selected SHA-256 content digest. |
| `--output` | Path | `None` | Write or copy output at this path. |

### `palimpsest layer pack`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest layer pack [-h] --tag TAG directory
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `directory` | Path | required | Directory to pack or verify. |
| `--tag` | string | required | Tag to assign to the produced artifact. |

### `palimpsest layer push`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest layer push [-h] [--name NAME] [--parent PARENT]
                             [--base-image BASE_IMAGE]
                             [--ubuntu-base UBUNTU_BASE] [--publish]
                             value
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `value` | string | required | Local layer tag or digest to upload. |
| `--name` | string | `None` | Managed object, run, profile, or publication name. |
| `--parent` | string | `None` | Record or filter by the parent layer digest. |
| `--base-image` | string | `None` | Cloud-image digest anchoring the layer chain. |
| `--ubuntu-base` | string | `None` | Filter or record the Ubuntu base release. |
| `--publish` | flag | `False` | Make the uploaded Hub artifact publicly visible. |

## `palimpsest bundle`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest bundle [-h] {pull,verify} ...
```

Subcommands: `pull`, `verify`.

### `palimpsest bundle pull`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest bundle pull [-h] --output OUTPUT [--include-base]
                              leaf_digest
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `leaf_digest` | string | required | Leaf layer digest of the bundle chain. |
| `--output` | Path | required | Write or copy output at this path. |
| `--include-base` | flag | `False` | Include the cloud-image base in the downloaded bundle. |

### `palimpsest bundle verify`

[Behavior, restrictions, and examples](usage.md#hub-cloud-images-layers-and-bundles).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest bundle verify [-h] directory
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `directory` | Path | required | Directory to pack or verify. |

## `palimpsest oci`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci [-h]
                      {init-runtime,materialize,root-proof,exec-status,exec-record,resource-status,root-volumes,root-volume} ...
```

Subcommands: `init-runtime`, `materialize`, `root-proof`, `exec-status`, `exec-record`, `resource-status`, `root-volumes`, `root-volume`.

### `palimpsest oci init-runtime`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci init-runtime [-h] path
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `path` | Path | required | Parent directory in which to create secured OCI runtime state. |

### `palimpsest oci materialize`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci materialize [-h] [--manifest MANIFEST]
                                  [--platform {linux/amd64}] [--packer PACKER]
                                  [--timeout TIMEOUT] [--output OUTPUT]
                                  source
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `source` | Path | required | Local OCI archive or image-layout source. |
| `--manifest` | string | `None` | pin a root descriptor; required when the local index is ambiguous |
| `--platform` | one of: `linux/amd64` | `'linux/amd64'` | Select an OCI platform. |
| `--packer` | Path | `/usr/bin/mksquashfs` | Absolute SquashFS packer executable (verified before use). |
| `--timeout` | float | `300.0` | Materializer timeout in seconds; must be positive and finite. |
| `--output` | Path | `None` | New file for the canonical materialization receipt; stdout if omitted. |

### `palimpsest oci root-proof`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci root-proof [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

### `palimpsest oci exec-status`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci exec-status [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

### `palimpsest oci exec-record`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci exec-record [-h] path
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `path` | string | required | Durable OCI exec completion-record path to validate and print. |

### `palimpsest oci resource-status`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci resource-status [-h]
```

### `palimpsest oci root-volumes`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci root-volumes [-h]
```

### `palimpsest oci root-volume`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest oci root-volume [-h] volume_id
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `volume_id` | string | required | Exact retained OCI root-volume UUID. |

## `palimpsest build`

[Behavior, restrictions, and examples](usage.md#build).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest build [-h] [--frontend {auto,palimpsestfile,dockerfile}]
                        [--base BASE] --tag TAG [-f RECIPE] [--layer LAYER]
                        [--network {none,default}] [--offline]
                        [--platform PLATFORM] [--target TARGET]
                        [--build-arg BUILD_ARG] [--local-image LOCAL_IMAGE]
                        [--cache-scope CACHE_SCOPE] [--registry REGISTRY]
                        [--cache-from CACHE_FROM] [--cache-to CACHE_TO]
                        [--no-cache] [--pull] [--load]
                        [--progress {auto,none,plain,quiet,rawjson,tty}]
                        [--output OUTPUT] [--rootfs-output ROOTFS_OUTPUT]
                        [--runtime-tag RUNTIME_TAG]
                        [--runtime-base RUNTIME_BASE]
                        [--runtime-block-size RUNTIME_BLOCK_SIZE] [--push]
                        [--runtime-push]
                        [context]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `context` | Path (optional) | `None` | Dockerfile build context directory. |
| `--frontend` | one of: `auto`, `palimpsestfile`, `dockerfile` | `'auto'` | Choose frontend explicitly or infer it from the other arguments. |
| `--base` | string | `None` | Legacy Palimpsestfile cloud-image base digest. |
| `--tag`, `-t` | string; repeatable | required | Tag to assign to the produced artifact. |
| `-f`, `--file` | Path | `None` | Build recipe path (Dockerfile or Palimpsestfile). |
| `--layer` | string; repeatable | `None` | Add a legacy layer digest; repeat to preserve chain order. |
| `--network` | one of: `none`, `default` | `'none'` | Select build or runtime networking. |
| `--offline` | flag | `False` | Enforce an offline Dockerfile build. |
| `--platform` | string | `None` | Select an OCI platform. |
| `--target` | string | `None` | Dockerfile build stage to target. |
| `--build-arg` | string; repeatable | `[]` | Set a Dockerfile build argument; repeatable. |
| `--local-image` | string; repeatable | `[]` | Add &#96;ALIAS=/absolute/layout@sha256:DIGEST&#96; as a verified local OCI build context; repeatable. |
| `--cache-scope` | string | `None` | Select the Palimpsest Hub cache scope. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |
| `--cache-from` | string; repeatable | `[]` | Add an external BuildKit cache source; repeatable. |
| `--cache-to` | string; repeatable | `[]` | Add an external BuildKit cache destination; repeatable. |
| `--no-cache` | flag | `False` | Disable cache reuse (offline builds only). |
| `--pull` | flag | `False` | Always resolve newer base images. |
| `--load` | flag | `False` | Load the build result into the local Docker image store. |
| `--progress` | one of: `auto`, `none`, `plain`, `quiet`, `rawjson`, `tty` | `'plain'` | Select BuildKit progress rendering. |
| `--output` | Path | `None` | OCI archive output path; an invocation-unique state path is chosen when omitted. |
| `--rootfs-output` | Path | `None` | Export the built root filesystem at this path. |
| `--runtime-tag` | string | `None` | Tag for the generated SquashFS runtime block. |
| `--runtime-base` | string | `None` | Base digest for the generated runtime-block chain. |
| `--runtime-block-size` | int | `None` | SquashFS block size: power of two from 4096 through 1048576 bytes (semantic default: 131072). |
| `--push` | flag | `False` | push the OCI image to its configured registry |
| `--runtime-push` | flag | `False` | push the SquashFS runtime block to Palimpsest Hub |

## `palimpsest registry`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry [-h] {ls,add,use,rm,inspect,buildkit-config} ...
```

Subcommands: `ls`, `add`, `use`, `rm`, `inspect`, `buildkit-config`.

### `palimpsest registry ls`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry ls [-h] [--format {table,json}]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--format` | one of: `table`, `json` | `'table'` | Select the output representation or Docker format template. |

### `palimpsest registry add`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry add [-h] [--namespace NAMESPACE] [--mirror MIRROR]
                               [--ca CA] [--plain-http] [--tls-skip-verify]
                               [--cache-from CACHE_FROM] [--cache-to CACHE_TO]
                               [--default] [--force]
                               name endpoint
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |
| `endpoint` | string | required | Registry hostname/endpoint. |
| `--namespace` | string | `None` | Namespace prepended to short image references. |
| `--mirror` | string; repeatable | `[]` | Add a registry mirror; repeatable. |
| `--ca` | Path; repeatable | `[]` | Add a trusted registry CA file; repeatable. |
| `--plain-http` | flag | `False` | Allow unencrypted HTTP for this registry. |
| `--tls-skip-verify` | flag | `False` | Disable TLS certificate verification for this registry. |
| `--cache-from` | string; repeatable | `[]` | Add an external BuildKit cache source; repeatable. |
| `--cache-to` | string; repeatable | `[]` | Add an external BuildKit cache destination; repeatable. |
| `--default` | flag | `False` | Make this the default registry profile. |
| `--force` | flag | `False` | Replace, remove, or overwrite despite the normal protection. |

### `palimpsest registry use`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry use [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

### `palimpsest registry rm`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry rm [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

### `palimpsest registry inspect`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry inspect [-h] [name]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string (optional) | `None` | Managed object, run, profile, or publication name. |

### `palimpsest registry buildkit-config`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest registry buildkit-config [-h] --output OUTPUT [--force]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--output` | Path | required | Write or copy output at this path. |
| `--force` | flag | `False` | Replace, remove, or overwrite despite the normal protection. |

## `palimpsest login`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest login [-h] [-u USERNAME] [--password-stdin]
                        [--registry REGISTRY]
                        [server]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `server` | string (optional) | `None` | Docker registry server or configured profile name. |
| `-u`, `--username` | string | `None` | Docker registry username. |
| `--password-stdin` | flag | `False` | Read the registry password from stdin. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

## `palimpsest logout`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest logout [-h] [--registry REGISTRY] [server]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `server` | string (optional) | `None` | Docker registry server or configured profile name. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

## `palimpsest pull`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest pull [-h] [-a] [--platform PLATFORM] [-q]
                       [--registry REGISTRY]
                       reference
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `reference` | string | required | Docker image reference or ID. |
| `-a`, `--all-tags` | flag | `False` | Operate on every tag; reference must be an untagged repository. |
| `--platform` | string | `None` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

## `palimpsest push`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest push [-h] [-a] [--platform PLATFORM] [-q]
                       [--registry REGISTRY]
                       reference
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `reference` | string | required | Docker image reference or ID. |
| `-a`, `--all-tags` | flag | `False` | Operate on every tag; reference must be an untagged repository. |
| `--platform` | string | `None` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

## `palimpsest tag`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest tag [-h] [--registry REGISTRY] source target
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `source` | string | required | Existing Docker image reference or ID. |
| `target` | string | required | New target reference, resolved through the selected registry profile. |
| `--registry` | string | `None` | Resolve the operation through this registry profile. |

## `palimpsest images`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest images [-h] [-a] [--digests] [-f FILTER] [--format FORMAT]
                         [--no-trunc] [--tree] [-q]
                         [repository]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `repository` | string (optional) | `None` | Optional repository filter. |
| `-a`, `--all` | flag | `False` | Include intermediate and dangling images. |
| `--digests` | flag | `False` | Show content digests. |
| `-f`, `--filter` | string; repeatable | `[]` | Add a Docker image-list filter; repeatable. |
| `--format` | string | `None` | Select the output representation or Docker format template. |
| `--no-trunc` | flag | `False` | Do not truncate Docker output. |
| `--tree` | flag | `False` | Show Docker's image tree view. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |

## `palimpsest history`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest history [-h] [--format FORMAT] [--no-trunc]
                          [--platform PLATFORM] [-q]
                          reference
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `reference` | string | required | Docker image reference or ID. |
| `--format` | string | `None` | Select the output representation or Docker format template. |
| `--no-trunc` | flag | `False` | Do not truncate Docker output. |
| `--platform` | string | `None` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |

## `palimpsest rmi`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest rmi [-h] [-f] [--no-prune] [--platform PLATFORM]
                      references [references ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `references` | string (one or more) | required | One or more Docker image references or IDs. |
| `-f`, `--force` | flag | `False` | Replace, remove, or overwrite despite the normal protection. |
| `--no-prune` | flag | `False` | Do not delete untagged parents. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |

## `palimpsest save`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest save [-h] [-o OUTPUT] [--platform PLATFORM]
                       references [references ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `references` | string (one or more) | required | One or more Docker image references or IDs. |
| `-o`, `--output` | Path | `None` | Write or copy output at this path. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |

## `palimpsest load`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest load [-h] [-i INPUT_PATH] [--platform PLATFORM] [-q]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `-i`, `--input` | Path | `None` | Read the Docker image archive from this file instead of stdin. |
| `--platform` | string; repeatable | `[]` | Select an OCI platform. |
| `-q`, `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |

## `palimpsest docker`

[Behavior, restrictions, and examples](usage.md#registry-profiles-and-docker-wrapper-commands).

`-h`/`--help` is forwarded to Docker; this pass-through parser has no Palimpsest help flag.

```text
usage: palimpsest docker ...
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `docker_args` | string (remainder) | `None` | Arguments passed verbatim to the Docker CLI. |

## `palimpsest run`

[Behavior, restrictions, and examples](usage.md#run-cloud-image-versus-oci-root).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest run [-h] --name NAME [--layer LAYER] [--memory MEMORY]
                      [--vcpus VCPUS] [--network NETWORK]
                      [--backend {auto,kvm,lima-vz,libvirt-hvf}]
                      [--runtime-kind {cloud-image,oci-root}] [-d]
                      [--manifest MANIFEST] [--user USER]
                      [--root-retention {delete,retain}]
                      [--root-volume ROOT_VOLUME]
                      image_or_bundle
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `image_or_bundle` | string | required | Cloud digest/bundle directory or local OCI archive/layout. |
| `--name` | string | required | Managed object, run, profile, or publication name. |
| `--layer` | string; repeatable | `[]` | Append a verified SquashFS layer digest to a cloud-image stack. |
| `--memory` | int | `4096` | Guest memory in MiB (valid range: 256–1,048,576). |
| `--vcpus` | int | `2` | Guest virtual CPUs (valid range: 1–256). |
| `--network` | string | `None` | Runtime network; OCI-root currently permits only &#96;none&#96;. |
| `--backend` | one of: `auto`, `kvm`, `lima-vz`, `libvirt-hvf` | `'auto'` | Select or automatically detect the runtime backend. |
| `--runtime-kind` | one of: `cloud-image`, `oci-root` | `None` | Disambiguate cloud-image and local OCI-root execution. |
| `-d`, `--detach` | flag | `False` | leave an OCI-root VM running after READY |
| `--manifest` | string | `None` | pin the root descriptor of a local OCI image |
| `--user` | string | `None` | run a local OCI image as USER[:GROUP] |
| `--root-retention` | one of: `delete`, `retain` | `None` | OCI writable-root removal policy (default: delete) |
| `--root-volume` | string | `None` | reuse an explicitly retained OCI writable-root UUID |

## `palimpsest compose`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose [-h] [-f PROJECT_FILE] [-p PROJECT_NAME]
                          [--project-directory PROJECT_DIRECTORY]
                          [--env-file ENV_FILE]
                          {config,up,down,ps,logs,exec,stop,port} ...
```

Subcommands: `config`, `up`, `down`, `ps`, `logs`, `exec`, `stop`, `port`.

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `-f`, `--file` | Path | `None` | Compose-shaped project file path. |
| `-p`, `--project-name` | string | `None` | Override the deterministic project name. |
| `--project-directory` | Path | `None` | Base directory for project-contained resources. |
| `--env-file` | Path; repeatable | `[]` | Load a project-contained interpolation file; repeatable. |

### `palimpsest compose config`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose config [-h] [--quiet] [--format {json}] [--services]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--quiet` | flag | `False` | Suppress normal detail or print identifiers only. |
| `--format` | one of: `json` | `'json'` | Select the output representation or Docker format template. |
| `--services` | flag | `False` | Optional service names; omission selects the command default set. |

### `palimpsest compose up`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose up [-h] [-d] [--no-recreate] [--force-recreate]
                             [services ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `services` | string (zero or more) | `None` | Optional service names; omission selects the command default set. |
| `-d`, `--detach` | flag | `False` | Return after startup instead of following workload/log output. |
| `--no-recreate` | flag | `False` | Keep existing matching service runs. |
| `--force-recreate` | flag | `False` | Replace existing service runs. |

### `palimpsest compose down`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose down [-h] [--volumes]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--volumes`, `-v` | flag | `False` | Also request removal of managed conventional/project volumes. |

### `palimpsest compose ps`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose ps [-h] [--format {table,json}] [services ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `services` | string (zero or more) | `None` | Optional service names; omission selects the command default set. |
| `--format` | one of: `table`, `json` | `'table'` | Select the output representation or Docker format template. |

### `palimpsest compose logs`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose logs [-h] [--follow] [services ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `services` | string (zero or more) | `None` | Optional service names; omission selects the command default set. |
| `--follow`, `-f` | flag | `False` | Continue streaming appended logs. |

### `palimpsest compose exec`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose exec [-h] service ...
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `service` | string | required | Project service name. |
| `command` | string (remainder) | `None` | Command and arguments passed to the managed workload. |

### `palimpsest compose stop`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose stop [-h] [services ...]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `services` | string (zero or more) | `None` | Optional service names; omission selects the command default set. |

### `palimpsest compose port`

[Behavior, restrictions, and examples](usage.md#compose-shaped-projects).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest compose port [-h] [--protocol {tcp,udp}]
                               service private_port
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `service` | string | required | Project service name. |
| `private_port` | int | required | Guest/private port number. |
| `--protocol` | one of: `tcp`, `udp` | `'tcp'` | Published transport protocol. |

## `palimpsest ps`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest ps [-h]
```

## `palimpsest inspect`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest inspect [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

## `palimpsest logs`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest logs [-h] [--follow] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |
| `--follow` | flag | `False` | Continue streaming appended logs. |

## `palimpsest shell`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest shell [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

## `palimpsest exec`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest exec [-h] [--completion-record COMPLETION_RECORD] name ...
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--completion-record` | string | `None` | Persist the OCI exec completion record at this path. |
| `name` | string | required | Managed object, run, profile, or publication name. |
| `command` | string (remainder) | `None` | Command and arguments passed to the managed workload. |

## `palimpsest start`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest start [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

## `palimpsest stop`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest stop [-h] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |

## `palimpsest rm`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest rm [-h] [--volumes] name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |
| `--volumes` | flag | `False` | Also request removal of managed conventional/project volumes. |

## `palimpsest commit`

[Behavior, restrictions, and examples](usage.md#runtime-lifecycle-and-observation).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest commit [-h] --tag TAG name
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `name` | string | required | Managed object, run, profile, or publication name. |
| `--tag` | string | required | Tag to assign to the produced artifact. |

## `palimpsest ui`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest ui [-h] [--port PORT] [--no-browser]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--port` | int | `0` | UI listen port; 0 selects an available port. |
| `--no-browser` | flag | `False` | Do not open the UI in a browser. |

## `palimpsest store`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store [-h] {show,ls,rm,move,set} ...
```

Subcommands: `show`, `ls`, `rm`, `move`, `set`.

### `palimpsest store show`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store show [-h] [--format {table,json}]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--format` | one of: `table`, `json` | `'table'` | Select the output representation or Docker format template. |

### `palimpsest store ls`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store ls [-h] [--kind {image,layer,all}]
                           [--format {table,json}]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--kind` | one of: `image`, `layer`, `all` | `'all'` | Filter by artifact kind. |
| `--format` | one of: `table`, `json` | `'table'` | Select the output representation or Docker format template. |

### `palimpsest store rm`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store rm [-h] [--force] digest
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `digest` | string | required | Expected or selected SHA-256 content digest. |
| `--force` | flag | `False` | Accepted for compatibility; does not bypass referenced-object or lease protections. |

### `palimpsest store move`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store move [-h] --to DESTINATION [--keep-source]
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--to` | Path | required | New local store path. |
| `--keep-source` | flag | `False` | Keep source bytes after moving the store. |

### `palimpsest store set`

[Behavior, restrictions, and examples](usage.md#local-store-and-ui).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest store set [-h] --to DESTINATION
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `--to` | Path | required | New local store path. |

## `palimpsest completion`

[Behavior, restrictions, and examples](usage.md#invocation-and-configuration).

Automatic `-h`/`--help` prints help for this command and exits.

```text
usage: palimpsest completion [-h] {zsh,bash,fish}
```

| Argument | Value | Parser default | Description |
| --- | --- | --- | --- |
| `shell` | one of: `zsh`, `bash`, `fish` | required | Shell completion target. |
